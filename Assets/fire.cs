using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fire : MonoBehaviour
{
   
   

    // Start is called before the first frame update
    void Start()
    {
        
        //bodymat = materials[Random.Range(0, 2)];
        
    }

    // Update is called once per frame
    void Update()
    {
        
       
    }
}
